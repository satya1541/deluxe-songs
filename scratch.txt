        {displayMode === 'floating-pill' ? (
          <div className={`floating-pill-bar ${isExitingPill ? 'is-exiting' : ''}`}>
            {/* Left Controls: Prev / Play / Next */}
            <div className="pill-left-controls">
              <button
                type="button"
                className="pill-icon-btn"
                onClick={onPrevSong}
                disabled={!hasPrev && currentTime < 3}
                title="Previous track"
              >
                <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M6 6h2v12H6zm3.5 6l8.5 6V6z" />
                </svg>
              </button>

              <button
                type="button"
                className="pill-play-btn"
                onClick={onTogglePlay}
                title={isPlaying ? 'Pause' : 'Play'}
              >
                {isPlaying ? (
                  <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
                    <rect x="6" y="5" width="4" height="14" rx="1" />
                    <rect x="14" y="5" width="4" height="14" rx="1" />
                  </svg>
                ) : (
                  <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor" style={{ marginLeft: 2 }}>
                    <path d="M8 5v14l11-7z" />
                  </svg>
                )}
              </button>

              <button
                type="button"
                className="pill-icon-btn"
                onClick={onNextSong}
                disabled={!hasNext}
                title="Next track"
              >
                <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M6 18l8.5-6L6 6v12zM16 6v12h2V6h-2z" />
                </svg>
              </button>
            </div>

            {/* Center: Song Thumbnail Capsule + Dancing EQ + Micro Progress */}
            <div className="pill-center-capsule" onClick={handleSeek}>
              <div className="capsule-thumb-wrap">
                <SongArtwork
                  cover={currentSong.cover}
                  name={currentSong.name}
                  songId={currentSong.id}
                  className="capsule-thumb-img"
                />
              </div>

              <div className="capsule-text-block">
                <span className="capsule-artist" title={currentSong.artist}>
                  {currentSong.artist}
                </span>
                <span className="capsule-title" title={currentSong.name}>
                  {currentSong.name}
                </span>
              </div>

              {/* Dancing Waveform EQ */}
              <div className={`capsule-eq ${isPlaying ? 'is-dancing' : ''}`}>
                <span className="eq-bar bar-1" />
                <span className="eq-bar bar-2" />
                <span className="eq-bar bar-3" />
              </div>

              {/* Micro Progress Bar */}
              <div className="capsule-progress-track">
                <div
                  className="capsule-progress-fill"
                  style={{
                    width: `${Math.min(100, (currentTime / (duration || currentSong.duration || 240)) * 100)}%`,
                  }}
                />
              </div>
            </div>

            {/* Right: Source Badge, Queue & Volume */}
            <div className="pill-right-controls">
              {currentSong.sourceBadge && (
                <span
                  className="pill-source-badge"
                  style={{
                    backgroundColor: currentSong.sourceBadge.bg,
                    color: currentSong.sourceBadge.color,
                    borderColor: currentSong.sourceBadge.border,
                  }}
                  title={`${currentSong.sourceBadge.name} (${currentSong.sourceBadge.qualityLabel})`}
                >
                  {currentSong.sourceBadge.icon}
                </span>
              )}

              {/* Queue Button */}
              <button
                type="button"
                className={`pill-icon-btn ${queueOpen ? 'active' : ''}`}
                onClick={() => setQueueOpen((o) => !o)}
                title="Up Next Queue"
              >
                <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
                  <path d="M15 6H3v2h12V6zm0 4H3v2h12v-2zM3 16h8v-2H3v2zM17 6v8.18c-.31-.11-.65-.18-1-.18-1.66 0-3 1.34-3 3s1.34 3 3 3 3-1.34 3-3V8h3V6h-5z" />
                </svg>
              </button>

              {/* Volume Button with Popover that NEVER closes while dragging */}
              <div
                className="pill-volume-wrapper"
                onMouseEnter={handlePillVolMouseEnter}
                onMouseLeave={handlePillVolMouseLeave}
              >
                <button
                  type="button"
                  className="pill-icon-btn"
                  onClick={handlePillVolClick}
                  title={isMuted || displayVolume === 0 ? 'Unmute' : `Volume: ${displayVolume}% (Click to adjust)`}
                >
                  {isMuted || displayVolume === 0 ? (
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
                      <path d="M16.5 12c0-1.77-1.02-3.29-2.5-4.03v2.21l2.45 2.45c.03-.2.05-.41.05-.63zm2.5 0c0 .94-.2 1.82-.54 2.64l1.51 1.51C20.63 14.91 21 13.5 21 12c0-4.28-2.99-7.86-7-8.77v2.06c2.89.86 5 3.54 5 6.71zM4.27 3L3 4.27 7.73 9H3v6h4l5 5v-6.73l4.25 4.25c-.67.52-1.42.93-2.25 1.18v2.06c1.38-.31 2.63-.95 3.69-1.81L19.73 21 21 19.73l-9-9L4.27 3zM12 4L9.91 6.09 12 8.18V4z" />
                    </svg>
                  ) : displayVolume < 50 ? (
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
                      <path d="M18.5 12c0-1.77-1.02-3.29-2.5-4.03v8.05c1.48-.73 2.5-2.25 2.5-4.02zM5 9v6h4l5 5V4L9 9H5z" />
                    </svg>
                  ) : (
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
                      <path d="M3 9v6h4l5 5V4L7 9H3zm13.5 3c0-1.77-1.02-3.29-2.5-4.03v8.05c1.48-.73 2.5-2.25 2.5-4.02zM14 3.23v2.06c2.89.86 5 3.54 5 6.71s-2.11 5.85-5 6.71v2.06c4.01-.91 7-4.49 7-8.77s-2.99-7.86-7-8.77z" />
                    </svg>
                  )}
                </button>

                {showPillVolume && (
                  <div
                    className="pill-volume-popover"
                    onClick={(e) => e.stopPropagation()}
                    onMouseEnter={handlePillVolMouseEnter}
                    onMouseLeave={handlePillVolMouseLeave}
                  >
                    <button
                      type="button"
                      onClick={toggleMute}
                      className="pill-volume-mute-btn"
                      title={isMuted ? 'Unmute' : 'Mute'}
                    >
                      {isMuted || displayVolume === 0 ? '🔇' : '🔊'}
                    </button>
                    <input
                      type="range"
                      min="0"
                      max="100"
                      value={isMuted ? 0 : displayVolume}
                      onMouseDown={() => setIsDraggingPillVolume(true)}
                      onMouseUp={() => setIsDraggingPillVolume(false)}
                      onTouchStart={() => setIsDraggingPillVolume(true)}
                      onTouchEnd={() => setIsDraggingPillVolume(false)}
                      onChange={(e) => handleVolumeChange(parseInt(e.target.value, 10))}
                      className="pill-volume-slider"
                      style={{
                        background: `linear-gradient(to right, #1ed760 0%, #1ed760 ${
                          isMuted ? 0 : displayVolume
                        }%, #4d4d4d ${isMuted ? 0 : displayVolume}%, #4d4d4d 100%)`,
                      }}
                    />
                    <span className="pill-volume-val">{isMuted ? 0 : displayVolume}%</span>
                  </div>
                )}
              </div>
            </div>
          </div>
        ) : (
          <>
